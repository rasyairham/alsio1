{
  "version": 2,
  "buildCommand": "npm install --prefix frontend --include=dev && cd frontend && ./node_modules/.bin/vite build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --prefix backend",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
